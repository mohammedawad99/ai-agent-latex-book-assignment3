# Prompts Log

## Purpose

This document records the AI prompts used during the project so the workflow is transparent and reviewable, as required by the course. It is a running log: entries are added as work proceeds. To keep it readable and avoid leaking noise, entries are written as concise **summaries** of each prompt and its intent rather than full raw transcripts.

## Prompt Logging Policy

- Log a summary entry for each meaningful AI-assisted task (a document, a component, a review pass).
- Record what was asked for, the important constraints we imposed, what artifact resulted, and the result of our review.
- Do not paste secrets, API keys, or environment values into this log.
- Do not claim an artifact exists or works if it does not. Entries describe only work actually performed.
- Where a tool/model is not yet decided, record it as such rather than guessing. Planning and foundation stages used a general AI coding assistant; the concrete pipeline LLM model/provider was deferred (per decision D-014) and later confirmed as Gemini `gemini/gemini-2.5-flash` (D-022). The first real CrewAI executions happened at Stage 8B.1 (minimal) and 8C.3 (full), run manually by the student; no LaTeX/PDF and no accepted full content exist yet.

## Prompt Record Template

Use this template for new entries:

| Field | Description |
| --- | --- |
| Stage | Project stage number |
| Date | Date of the prompt (YYYY-MM-DD) |
| Goal | What the prompt aimed to achieve |
| Tool/Model | The AI tool/model used, if known |
| Prompt summary | A short description of what was asked |
| Important constraints | Key rules imposed (e.g. docs-only, no code, no fake evidence) |
| Output artifact | The file(s) produced or changed |
| Review result | Outcome of group review (accepted / revised / rejected) |

## Logged Prompts (Stages 0–9.5)

These entries summarize the AI-assisted work already completed. They are summaries, not raw prompts. Stages 0–4 produced repository structure and planning documentation; Stages 5–7 added project setup, the deterministic foundation, and the offline CrewAI core. **Stages 8B.1 and 8C.3 included real Gemini runs executed manually by the student in their own terminal** — a minimal run (8B.1) and the first full content run (8C.3) — and committed evidence exists for both under `results/`. The first full content evidence exists but is **rejected for final PDF use** (wrong topic, placeholders, ~10 pages; see D-024). Stage 8C.6 itself is **offline only** — it performs no real run, no `kickoff`, no LLM/API call, and creates no evidence. No LaTeX/PDF exists yet, and no accepted full content exists yet.

| Stage | Date | Goal | Tool/Model | Prompt summary | Important constraints | Output artifact | Review result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 0 | 2026-06-11 | Create a clean repository skeleton | AI coding assistant | Asked to create the directory structure, placeholder docs, `.gitignore`, and package init, with no implementation | Skeleton only; no code; no fake outputs; preserve empty dirs | Repository skeleton (commit `a0f8734`) | Accepted |
| 1 | 2026-06-11 | Write a professional PRD | AI coding assistant | Asked to write a full PRD (problem, goals, scope, requirements, acceptance criteria, traceability) from the assignment brief | Plan only; no implementation; evidence-first; group submission | `docs/PRD.md` (commit `b501a36`) | Accepted after targeted submission fixes |
| 2 | 2026-06-11 | Write a technical PLAN | AI coding assistant | Asked to design the architecture and strategy strictly from the PRD | Design only; no code/deps/LaTeX; do not invent features | `docs/PLAN.md` (commit `6baece3`) | Accepted |
| 3 | 2026-06-11 | Write a detailed staged TODO | AI coding assistant | Asked to break the PLAN into staged, actionable checklist tasks | Task plan only; ≥120 checkboxes; match PLAN roadmap | `docs/TODO.md` (commit `a722f64`) | Accepted after quality-timing correction |
| 4 | 2026-06-11 | Write supporting documentation | AI coding assistant | Asked to write AI_WORKFLOW, PROMPTS, DECISIONS, COSTS, QUALITY, SUBMISSION_CHECKLIST, and an initial README | Docs only; planned-status wording; no implementation claims | This document and the other Stage 4 files | Accepted for initial Stage 4 documentation after review |
| 5 | 2026-06-11 | Project setup (uv, deps, CLI skeleton) | AI coding assistant | Asked to create `pyproject.toml`, uv lock, `.env.example`, config, a safe CLI skeleton, and a smoke test | Setup only; no agents/LLM; no secrets; pytest+ruff pass | Stage 5 setup files | Accepted (commit `d21b3c7`) |
| 6 | 2026-06-11 | Deterministic foundation modules | AI coding assistant | Asked to implement config/paths/evidence/logging/runtime modules and their offline tests | Deterministic only; no CrewAI/LLM; temp-only evidence; files under the line limit | `config.py`, `paths.py`, `evidence.py`, `logging_setup.py`, `runtime.py`, and their tests | Accepted (commit `3e538af`) |
| 7 | 2026-06-11 | CrewAI core (offline specs + schemas + blueprint) | AI coding assistant | Asked to define agent/task specs, output-schema specs, a validated dry-run blueprint, a deferred object builder, and a `crew-plan` CLI command | Offline only; no kickoff; no LLM/API; no real run/evidence | `crew/specs.py`, `crew/schemas.py`, `crew/agents.py`, `crew/tasks.py`, `crew/builder.py`, CLI `crew-plan`, and tests | Accepted (commit `aeb39dc`) |
| 8A | 2026-06-11 | Controlled-run scaffolding (no real run) | AI coding assistant | Asked to add the LLM resolver, minimal runner, `run-minimal` CLI with an explicit `--real` gate, and offline safety tests | Offline only; OpenAI-only; env-only secrets; no `.env` auto-load; no kickoff/LLM/API; no real run/evidence | `config.py`, `crew/llm.py`, `crew/runner.py`, `cli.py`, `cli_commands.py`, tests, and docs | Accepted (commit `d9acbf7`) |
| 8A.1 | 2026-06-11 | Add Gemini provider support (offline) | AI coding assistant | Asked to extend the resolver to support `provider="gemini"` with `GEMINI_API_KEY`/`GOOGLE_API_KEY`, plus offline tests | Offline only; env-only secrets; no `.env` auto-load; no kickoff/LLM/API; no dependency added; no real run/evidence | `crew/llm.py`, `tests/test_crew_llm.py`, and docs | Accepted (commit `a31f80a`) |
| 8B.0 | 2026-06-11 | Install Gemini provider dependency (offline) | AI coding assistant | Asked to add `crewai[google-genai]` via uv and an offline Gemini construction test | Dependency only; offline; no kickoff/LLM/API; no real run/evidence; no source-logic change | `pyproject.toml`, `uv.lock`, `tests/test_crew_llm.py`, and docs | Accepted (commit `7e28f01`) |
| 8B.1 | 2026-06-11 | Commit first real minimal Gemini run evidence | AI coding assistant | Asked to review and commit the evidence the student produced by running `run-minimal --real` against Gemini | Student executed the real run manually with their own key in their own terminal; no key logged or used by the assistant; evidence reviewed (secret scan clean) and committed; assistant ran no real call | `results/stage8b1-minimal-gemini-20260611-154559/` (evidence only) | Accepted (commit `81e5b26`) |
| 8C.1 | 2026-06-11 | Offline full content-pipeline scaffolding | AI coding assistant | Asked to add the full runner, per-task persistence, the `run-full` CLI, and offline tests | Offline only; dry-run default; single new `kickoff` only in the full real path; no real run/LLM/API/evidence; no dependency added | `crew/full_runner.py`, `crew/persist.py`, `cli.py`, `cli_commands.py`, `tests/test_full_runner.py`, and docs | Accepted (commit `20e1774`) |
| 8C.4 | 2026-06-11 | Commit first full Gemini run evidence | AI coding assistant | Asked to review and commit the evidence the student produced by running `run-full --real` against Gemini | Student executed the real run manually with their own key; no key logged/used by the assistant; evidence reviewed (secret scan clean) and committed; assistant ran no real call | `results/stage8c3-full-gemini-20260611-164153/` (evidence only) | Evidence accepted (commit `cf89e51`); content **rejected for final PDF** (wrong topic, placeholders, ~10 pages) |
| 8C.5 | 2026-06-11 | Document the rejected first full run | AI coding assistant | Asked to update docs to record the full run worked technically but its content is rejected; next step is prompt/config hardening | Docs only; no real run/LLM/API; no evidence/source/dependency change | `docs/TODO.md`, `docs/PROMPTS.md`, `docs/COSTS.md`, `docs/QUALITY.md`, `docs/DECISIONS.md` | Accepted (commit `c2127f2`) |
| 8C.6 | 2026-06-11 | Offline topic/metadata hardening | AI coding assistant | Asked to bind the crew to the configured topic and cover metadata and forbid the failure modes; add offline content checks and tests | Offline only; no real run/`kickoff`/LLM/API; no evidence; no dependency added; files under the line limit | `config/default.toml`, `config.py`, `crew/context.py`, `crew/instructions.py`, `crew/tasks.py`, `crew/builder.py`, `crew/full_runner.py`, `crew/content_checks.py`, tests, and docs | Accepted (commit `0858c44`) |
| 8C.7 | 2026-06-11 | Commit second full Gemini candidate evidence | AI coding assistant | Asked to review and commit the evidence the student produced by running `run-full --real` against Gemini after the 8C.6 hardening | Student executed the real run manually with their own key; no key logged/used by the assistant; evidence reviewed (secret scan clean, content_checks basic gate passes) and committed; assistant ran no real call | `results/stage8c7-full-gemini-20260611-173125/` (evidence only) | Candidate evidence accepted (commit `0611993`); **not final PDF-ready** (questionable citations, unsupported metrics, conceptual headers/footers) |
| 8C.7.1 | 2026-06-12 | Document second full candidate run | AI coding assistant | Asked to update docs to record the candidate run, its measured cost, and the remaining content-QA risks; next is Stage 8C.8 cleanup | Docs only; no real run/`kickoff`/LLM/API; no evidence/source/dependency change | `docs/TODO.md`, `docs/PROMPTS.md`, `docs/COSTS.md`, `docs/QUALITY.md`, `docs/DECISIONS.md` | Accepted (commit `0489cc5`) |
| 8C.8.0 | 2026-06-12 | Content-QA cleanup plan (plan only) | AI coding assistant | Asked to plan deterministic cleanup of the candidate into final source content without editing evidence; define rules, derived location, checks, and stages | Plan/docs only; evidence immutable; no cleaned content/LaTeX/PDF created; no real run/`kickoff`/LLM/API; no source/dependency change | `docs/TODO.md`, `docs/DECISIONS.md`, `docs/QUALITY.md`, `docs/PROMPTS.md`, `docs/COSTS.md` | Accepted (commit `dd18878`) |
| 8C.8.1 | 2026-06-12 | Deterministic offline QA scanner | AI coding assistant | Asked to implement an offline content QA scanner (forbidden/required string checks) with a `content-qa` CLI and tests; the 8C.7 candidate must fail on its known risks | Offline only; pure string checks; no network/model/API key; no file mutated; no real run/`kickoff`; no dependency added; files under the line limit | `content_qa.py`, `cli.py`, `cli_commands.py`, `tests/test_content_qa.py`, and docs | Accepted (commit `dcac0eb`) |
| 8C.8.1.1 | 2026-06-12 | Docs hash update after QA scanner commit | AI coding assistant | Asked to update docs only to record the actual commit `dcac0eb` for Stage 8C.8.1 and mark Stage 8C.8.2 as the next stage | Docs only; no real run/`kickoff`/LLM/API; no evidence/source/tests/config/dependency change; no cleaned content/LaTeX/PDF | `docs/TODO.md`, `docs/PROMPTS.md` | Accepted (commit `fc24be5`) |
| 8C.8.2 | 2026-06-12 | Create cleaned Markdown content from candidate | AI coding assistant | Asked to derive cleaned `book.md` + `README.md` under `latex_project/content/` from the 8C.7 candidate evidence, removing the 8 known risks, using only measured run values, and a conservative bibliography; must pass `content-qa` | Derived content only; evidence immutable (not edited); no LaTeX/PDF created; no real run/`kickoff`/LLM/API; no source/test/config/dependency change; scanner `ok: True`, exit 0 | `latex_project/content/book.md`, `latex_project/content/README.md`, and docs | Accepted (commit `98a50f2`) |
| 8C.8.3.1 | 2026-06-12 | Docs hash update after cleaned content commit | AI coding assistant | Asked to update docs only to record commit `98a50f2` for the cleaned Markdown content (committed and pushed after human review, including the minor citation/source fixes) and mark Stage 9.x (LaTeX assembly) as the next stage | Docs only; no real run/`kickoff`/LLM/API; no evidence/source/tests/config/dependency change; no LaTeX/PDF | `docs/TODO.md`, `docs/PROMPTS.md`, `docs/QUALITY.md` | Accepted (commit `650320d`) |
| 9.0 | 2026-06-12 | LaTeX assembly plan from cleaned Markdown (plan only) | AI coding assistant | Asked to plan (not implement) the conversion of `latex_project/content/book.md` into a polished ~15-page LaTeX article PDF: structure, engine (XeLaTeX vs LuaLaTeX), packages, Markdown→LaTeX mapping, figures/graph, bibliography, headers/footers, page target, build commands, validation gates, and sub-stages 9.1–9.6 | Plan/docs only; no `.tex`/`.bib`/image/script/PDF created; no compile attempted; no real run/`kickoff`/LLM/API; no evidence/content/source/tests/config/dependency change; engine grounded in verified facts (DejaVu Sans present via `fc-list`, TeX binaries absent via `which`) | `docs/TODO.md`, `docs/PROMPTS.md`, `docs/QUALITY.md`, `docs/DECISIONS.md` (D-030), `docs/COSTS.md` | Accepted (commit `c3ca705`) |
| 9.1 | 2026-06-12 | LaTeX skeleton and bibliography setup | AI coding assistant | Asked to (9.1.0) check the local TeX environment without installing anything and (9.1.1) create the LaTeX skeleton only: `main.tex` (cover/abstract/ToC/chapter includes/bibliography), `preamble.tex` (XeLaTeX, fontspec, polyglossia English+Hebrew with DejaVu Sans, geometry, fancyhdr headers/footers, hyperref, biblatex/biber), `references.bib` transcribed from the cleaned bibliography, nine skeleton chapter files (headings + TODO markers, citation commands, minimal BiDi snippet), `figures/`+`scripts/` placeholders, and `latex_project/README.md` | Skeleton only — no full chapter conversion, no figures/graph scripts, no compile (TeX Live absent: `xelatex`/`biber` missing; install command documented for the user), **no PDF**; no real run/`kickoff`/LLM/API; no evidence/content/source/tests/config/dependency change | `latex_project/main.tex`, `preamble.tex`, `references.bib`, `README.md`, `chapters/*.tex` (9 files), `figures/.gitkeep`, `scripts/.gitkeep`, and docs | Accepted (commit `af71265`) |
| 9.1.1 | 2026-06-12 | Docs hash update after LaTeX skeleton commit | AI coding assistant | Asked to update docs only to record commit `af71265` for the Stage 9.1 LaTeX skeleton and mark Stage 9.2 (figure/graph generation scripts) as the next stage | Docs only; no latex_project change; no compile/install (TeX Live still absent); no PDF/images/scripts; no real run/`kickoff`/LLM/API; no evidence/source/tests/config/dependency change | `docs/TODO.md`, `docs/PROMPTS.md` | Accepted (commit `1146acf`) |
| 9.2 | 2026-06-12 | Figure and graph generation scripts | AI coding assistant | Asked to create two small deterministic matplotlib scripts under `latex_project/scripts/` and generate the two mandatory images under `latex_project/figures/`: the Chapter 3 architecture block diagram (`architecture.png`) and the Chapter 6 illustrative generation-time chart (`generation_time.png`, y-axis "Illustrative generation time (minutes)", explicitly not measured); uncomment the chapter figure blocks now that the PNGs exist; update `latex_project/README.md` | Scripts run via `uv run --with matplotlib` — matplotlib NOT added to `pyproject.toml`/`uv.lock`; PNG outputs only (no figure PDFs); no compile/TeX install, **no final PDF**; no real run/`kickoff`/LLM/API; no evidence/content/source/tests/config/dependency change | `latex_project/scripts/generate_architecture.py`, `scripts/generate_graph.py`, `figures/architecture.png`, `figures/generation_time.png`, `chapters/03_architecture.tex`, `chapters/06_results.tex`, `latex_project/README.md`, and docs | Accepted (commit `6aa2cf1`) |
| 9.2.1 | 2026-06-12 | Docs hash update after figure generation commit | AI coding assistant | Asked to update docs only to record commit `6aa2cf1` for the Stage 9.2 figures and mark Stage 9.3 (convert cleaned Markdown into full LaTeX chapters) as the next stage | Docs only; no latex_project change; no compile/install (TeX Live still absent); no PDF/build artifacts; no real run/`kickoff`/LLM/API; no evidence/source/tests/config/dependency change | `docs/TODO.md`, `docs/PROMPTS.md` | Accepted (commit `f420c32`) |
| 9.3 | 2026-06-12 | Convert cleaned Markdown into full LaTeX chapters | AI coding assistant | Asked to convert `latex_project/content/book.md` (single source of truth) into the nine chapter files: faithful prose, itemize/enumerate lists, booktabs tables 4.1/6.1, equations `eq:agent_utility`/`eq:quality`, code listings (plain `verbatim` — no new packages), the rendered Hebrew–English BiDi passage via the existing polyglossia setup, citations matching `references.bib`, measured values exact (`gemini/gemini-2.5-flash`, 300.68 s, 213,664/235,436 tokens, 4 calls), unmeasured metrics labelled proposed, graph values labelled illustrative | Conversion only — no compile/TeX install, **no PDF**, no build artifacts; `main.tex`/`preamble.tex`/`references.bib`/scripts/figures untouched; no real run/`kickoff`/LLM/API; no evidence/content/source/tests/config/dependency change | `latex_project/chapters/*.tex` (9 files), `latex_project/main.tex` (approved `\nocite`), `latex_project/README.md`, and docs | Accepted (commit `38f8c86`) |
| 9.3.1 | 2026-06-12 | Docs hash update after LaTeX chapter conversion commit | AI coding assistant | Asked to update docs only to record commit `38f8c86` for the Stage 9.3 chapter conversion and mark Stage 9.4 (first compile; requires the manual TeX Live install) as the next stage | Docs only; no latex_project change; LaTeX sources content-complete but no compile/install (TeX Live still absent), **no PDF**; no real run/`kickoff`/LLM/API; no evidence/source/tests/config/dependency change | `docs/TODO.md`, `docs/PROMPTS.md` | Accepted (commit `3f5737a`) |
| 9.4 | 2026-06-12 | First LaTeX compile and compile-error fixes | AI coding assistant | Asked to verify the manually installed TeX Live toolchain (9.4.0 first stopped on missing `bidi.sty`; resolved by the student installing `texlive-lang-arabic`), run the XeLaTeX → biber → XeLaTeX ×2 sequence into the gitignored `build/`, and fix only log-proven compile blockers | Compile succeeds: `build/main.pdf` (19 pages) is a **build artifact only — not committed, not final**; fixes were minimal and log-driven: `preamble.tex` package order (hyperref → polyglossia/bidi → biblatex; both orderings demanded by explicit errors) and `\path` for two long bibliography paths (severe overfulls); final log has no errors / undefined refs; 5 minor overfulls (≤28 pt) + page count (19 vs 13–17) deferred to Stage 9.5; no real run/`kickoff`/LLM/API; no evidence/content/source/tests/config/dependency change | `latex_project/preamble.tex`, `latex_project/references.bib`, and docs | Accepted (commit `c7ce915`) |
| 9.4.1 | 2026-06-12 | Docs hash update after first compile commit | AI coding assistant | Asked to update docs only to record commit `c7ce915` for the Stage 9.4 compile fixes and mark Stage 9.5 (PDF validation/polish: page count 19→13–17, 5 minor overfulls, text-layer gates, needs `poppler-utils`) as the next stage | Docs only; no latex_project change; `build/main.pdf` remains a gitignored local artifact (not committed, not final); no real run/`kickoff`/LLM/API; no evidence/source/tests/config/dependency change | `docs/TODO.md`, `docs/PROMPTS.md`, `docs/QUALITY.md` | Accepted (commit `c117a2f`) |
| 9.5 | 2026-06-12 | PDF validation and polish | AI coding assistant | Asked to verify `poppler-utils`, run the deterministic PDF gates against a clean rebuild, apply minimal content-preserving polish to reach 13–17 pages and remove the overfull warnings, recompile after each change, and validate again | Result: **16 pages, 0 overfulls, log clean, all 14 text-layer gates pass, 0 `??`**, Hebrew BiDi in text layer + visually confirmed; polish = 2.2 cm margins, 10pt, `\small` verbatim/Table 6.1, `enumitem` compact lists, `\path` ×2, biblatex URL penalties, figure widths 0.5/0.7; `build/main.pdf` stays a gitignored artifact (not committed, not copied to `results/`, **no final-PDF/Moodle claim**); no real run/`kickoff`/LLM/API; no evidence/content/source/tests/config/dependency change | `latex_project/main.tex`, `preamble.tex`, `chapters/00_abstract.tex`, `chapters/03_architecture.tex`, `chapters/06_results.tex`, and docs | Accepted (commit `899ebb8`) |
| 9.5.1 | 2026-06-12 | Docs hash update after PDF validation commit | AI coding assistant | Asked to update docs only to record commit `899ebb8` for the Stage 9.5 validation/polish (16 pages, 0 overfulls, clean log, 14/14 text-layer gates) and mark Stage 9.6 (final PDF evidence to `results/final_pdf/`) as the next stage | Docs only; no latex_project change; `build/main.pdf` remains a gitignored local artifact (not committed; final evidence not yet copied to `results/final_pdf/`; Moodle PDF not ready); no real run/`kickoff`/LLM/API; no evidence/source/tests/config/dependency change | `docs/TODO.md`, `docs/PROMPTS.md`, `docs/QUALITY.md` | Accepted (commit pending; hash recorded in this commit) |

## Future Prompts

Project setup (Stage 5) and the deterministic foundation including the config loader (Stage 6) are already logged in the table above. Further implementation-stage prompts will be added here as the project proceeds — for example prompts used to draft the crew builder, the LaTeX assembler, the figure generator, the PDF builder, and the validation gates. No claim is made that any not-yet-built component exists. This log will grow alongside the actual work.
